
\documentclass[tikz,border=5pt]{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{pgfplotstable}

\begin{document}
    
    % Read CSV once with the correct separator:
    \pgfplotstableread[col sep=comma]{failed_monitors_count_per_mutant-every_1-th_entry.csv}\datatable
    
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=1pt,
            title={Failed Monitors per Mutant (every $1^{th}$ entry)},
            xlabel={Mutant},
            ylabel={Failed Monitors},
            enlargelimits=0.02,
            ymin=0,
            xtick=data,
            xticklabel style={rotate=45, anchor=east},
            xticklabels from table={\datatable}{mutantId},
            tick label style={font=\small},
            scaled y ticks=false,
            ]
            \addplot table[
            x expr=\coordindex,
            y=count,
            ]{\datatable};
        \end{axis}
    \end{tikzpicture}
    
\end{document}
    